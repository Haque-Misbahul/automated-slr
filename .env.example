# We will fill these TOMORROW.
BACKEND=inference            # or: mlx | transformers
MODEL_ID=Qwen/Qwen2.5-7B-Instruct
HF_TOKEN=                    # only needed if BACKEND=inference

# Embedding model for synonyms/taxonomy (tomorrow)
EMBED_MODEL=sentence-transformers/all-MiniLM-L6-v2
