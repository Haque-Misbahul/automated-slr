# ============================================================
# Core Web App Framework
# ============================================================
streamlit>=1.36.0         # UI framework for the SLR automation tool
watchdog>=4.0.1           # Improves Streamlit file-watcher performance (optional but recommended)

# ============================================================
# Environment & HTTP Utilities
# ============================================================
python-dotenv>=1.0.1      # For .env configuration loading
requests>=2.32.3          # For HTTP requests to APIs

# ============================================================
# NLP + Embeddings
# ============================================================
nltk>=3.9                 # Tokenization / stopword removal (optional for older code)
sentence-transformers>=5.1.0  # SBERT embeddings for synonym verification
transformers>=4.44.0      # Required by sentence-transformers >=5.x
scikit-learn>=1.4.0       # Needed by sentence-transformers for similarity computations
scipy>=1.12.0             # Core dependency for cosine similarity
torch==2.2.2              # Deep learning backend for SBERT (keep fixed for Mac compatibility)

# ============================================================
# LLM / API Clients
# ============================================================
openai>=1.40.0            # For GPT / gpt-oss integration layer
tiktoken>=0.7.0           # Token counting and prompt management helper

# ============================================================
# Visualization & Graphs
# ============================================================
graphviz>=0.20.3          # Taxonomy tree diagrams
plotly>=5.22.0            # Interactive charts (treemap, sunburst)
kaleido>=0.2.1            # Static Plotly image export

# ============================================================
# DataFrame & Compatibility Layer
# ============================================================
pandas>=2.0.0             # Core DataFrame library
narwhals>=1.10.0          # Plotly/Pandas/Polars bridge (internal fix dependency)
pyarrow>=16.0.0           # For Parquet/Arrow data export and caching

# ============================================================
# Optional Utilities / Local Development
# ============================================================
tqdm>=4.66.0              # Progress bars for background processes
protobuf>=5.27.0          # Streamlit internal dependency safeguard
typing_extensions>=4.10.0 # Consistent typing support for Python 3.9+

# ============================================================
# Note:
# - NumPy <2.0 still recommended until torch and Plotly fully stabilize on macOS.
# - LibreSSL warning is benign; ignore or upgrade Python via Homebrew if needed.
# ============================================================
numpy<2.0
